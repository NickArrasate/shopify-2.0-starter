<article class="site-page" data-template-page>

  {% if  page.metafields["global"]["hero_image"] %}
  {% section 'static-page-intro-meta' %}
  {% else %}
  <header class="page-masthead">
    <h1 class="page-title">
      {{ page.title }}
    </h1>
  </header>
{% endif %}
    <div class="page-content rte">
      {{ page.content }}
      {% section 'calendar-blocks' %}
    </div>
</article>
